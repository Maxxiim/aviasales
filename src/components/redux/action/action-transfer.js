export const all = (value) => ({ type: "all", payload: value });

export const no_transfer = (value) => ({ type: "no_transfer", payload: value });

export const one_transfer = (value) => ({
  type: "one_transfer",
  payload: value,
});

export const two_transfer = (value) => ({
  type: "two_transfer",
  payload: value,
});

export const three_transfer = (value) => ({
  type: "three_transfer",
  payload: value,
});
